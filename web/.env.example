# API 调用模式：
#   mock = 开发态 Mock 中间件（默认，按 path 分发，仅 vite serve 生效）
#   agc  = 真实 AGC 云函数网关（URL = ${VITE_API_BASE_URL}/${functionName}，body 自动合并 orgId/userId）
VITE_API_MODE=mock
# 统一 API Client 网关地址（AGC API Gateway / Cloud Functions 前缀）
VITE_API_BASE_URL=/api
# 请求超时（毫秒）
VITE_API_TIMEOUT_MS=30000
